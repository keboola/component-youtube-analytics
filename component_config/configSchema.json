{
  "title": "Youtube Reports",
  "type": "object",
  "required": [
    "input_variant",
    "report_types"
  ],
  "properties": {
    "#api_token": {
      "type": "string",
      "title": "API token",
      "description": "Just a temporary solution prior to having OAuth authorization ready",
      "format": "password",
      "propertyOrder": 10
    },
    "history_days": {
      "type": "integer",
      "title": "Maximum history days to include",
      "description": "You may reduce history to avoid report read quota problem (zero means no limit)",
      "propertyOrder": 20
    },
    "input_variant": {
      "type": "string",
      "title": "Content owner specification",
      "propertyOrder": 100,
      "description": "You may choose to generate reports for login user (default) or for selected content owner",
      "enum": [
        "use_login_owner",
        "use_selected_owner"
      ],
      "options": {
        "enum_titles": [
          "Login user is also the content owner",
          "Selected content owner"
        ]
      },
      "default": "use_login_owner"
    },
    "content_owner": {
      "type": "string",
      "title": "Content owner",
      "propertyOrder": 200,
      "minLength": 1,
      "description": "Enter ID of selected content owner.",
      "options": {
        "dependencies": {
          "input_variant": "use_selected_owner"
        }
      }
    },
    "report_types": {
      "type": "array",
      "title": "Report types",
      "propertyOrder": 200,
      "minLength": 1,
      "description": "Enter ID of selected content owner.",
      "format": "select",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": []
      },
      "options": {
        "async": {
          "label": "Load report types",
          "action": "list_report_types"
        }
      }
    },
    "debug": {
      "type": "boolean",
      "propertyOrder": 700,
      "title": "Debug",
      "hidden": true,
      "description": "When checked, logging will be more verbose",
      "format": "checkbox"
    }
  }
}